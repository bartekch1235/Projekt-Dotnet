@page
@model Projekt_Dotnet.Pages.harmonogram.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Harmonogram[0].HowManyDays)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Harmonogram[0].HowManyHours)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Harmonogram[0].People)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Harmonogram[0].Duties)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Harmonogram) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HowManyDays)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HowManyHours)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.People)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Duties)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Zglos sie</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
